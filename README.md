# Harvest

Web frontend, built with Next.js (App Router) and shipped as a static export.

## Development

```bash
npm install
npm run dev      # http://localhost:3000
npm run build    # production build
```

Runtime configuration is supplied via environment variables; copy
`.env.local.example` to `.env.local` and fill in the values for your
environment. Deployment-specific values are provided separately.
