export const dynamic = "force-static";
// Twitter card reuses the per-product OG image (summary_large_image
// is the same 1200x630 canvas).

export {
  default,
  size,
  contentType,
  alt,
  generateStaticParams,
} from "./opengraph-image";
